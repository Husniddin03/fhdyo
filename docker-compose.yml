version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fhdyo-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./fhdyo:/var/www/html
    networks:
      - laravel

  mysql:
    image: mysql:8.0
    container_name: fhdyo-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: fhdyo
      MYSQL_USER: fhdyo
      MYSQL_PASSWORD: fhdyo
    ports:
      - "3305:3306"
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - laravel

  nginx:
    image: nginx:latest
    container_name: fhdyo-nginx
    restart: unless-stopped
    ports:
      - "8003:80"
    volumes:
      - ./fhdyo:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - laravel

volumes:
  db_data:

networks:
  laravel:
    driver: bridge
